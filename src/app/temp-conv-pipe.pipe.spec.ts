import { TempConvPipePipe } from './temp-conv-pipe.pipe';

describe('TempConvPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TempConvPipePipe();
    expect(pipe).toBeTruthy();
  });
});
